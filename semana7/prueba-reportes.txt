Cuando se envía un reporte mediante la ruta POST /reportes, la información se almacena temporalmente en el array llamado reportes. Al consultar la ruta GET /reportes, el servidor devuelve la lista de todos los reportes registrados mientras el servidor permanece encendido. Esto permite verificar que los datos fueron guardados correctamente en memoria.
